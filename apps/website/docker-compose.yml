services:
  app:
    build: .
    container_name: windowdrop-web
    restart: unless-stopped
    env_file: .env
    ports:
      - "3410:3410"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3410/healthz"]
      interval: 10s
      timeout: 3s
      retries: 5

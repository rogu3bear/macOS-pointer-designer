# Analytics

## Website
This static website does not include any client-side analytics trackers (Google Analytics, etc.) by default. Server-side logs from the hosting provider (e.g., Cloudflare) can be used for basic traffic stats.

## App
WindowDrop diagnostics are local. No telemetry is sent unless the user explicitly shares a debug log via email.
